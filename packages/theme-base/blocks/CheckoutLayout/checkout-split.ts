/**
 * Split-чекаут: ОДИН источник разметки и стилей для пяти тем и для превью.
 *
 * Эталон владельца (третий круг тестировщика, п.4 «очень плохо работают
 * цветовые схемы» + уточнение после него): ПРАВАЯ колонка — поверхность,
 * покрашенная цветовой схемой «Сводки заказа»: сплошной цвет от верха до низа
 * окна и до правого края экрана, без полей и зазоров; текст и цифры на ней
 * берут цвет из той же схемы.
 *
 * ЛЕВАЯ колонка — НАША, темы. Дословно: «Левая часть от нас. Там только
 * меняется цвет кнопки и юр инфа цвет. Всё остальное наше. А правая часть как
 * в скрине». Поэтому её поверхность красится палитрой СТРАНИЦЫ (`--color-bg`
 * темы, без класса схемы), а из схемы формы левая колонка берёт ровно один
 * элемент — кнопку оформления (класс схемы садится на корень «Кнопки оплаты»,
 * см. CheckoutForm.astro). Второй «схемный» элемент низа — правовая полоса
 * `CheckoutFooterStrip`, у неё своя схема (секция «Подвал» страницы чекаута).
 *
 * Замер снятия заливки — собранные витрины пяти тем, Chromium, 1440×900 и
 * 390×844, корзина 1/3/30, 13-14.09. Колонка формы 0..720:
 *                 ДО (схема формы красила)              ПОСЛЕ (все схемы)
 *   rose      scheme-3 245,240,235 / scheme-4 0,0,0      255,255,255
 *   flux      scheme-3 250,250,250 / scheme-1 0,0,0      255,255,255
 *   satin     scheme-3 245,245,245 / scheme-4 8,2,0      255,255,255
 *   bloom     scheme-3 255,255,255 / scheme-1 207,122,139 255,255,255
 *   vanilla   scheme-3 238,238,238 / scheme-4 255,255,255 58,69,48
 * «ПОСЛЕ» совпадает со значением, которое замер даёт при НЕ выбранной схеме —
 * то есть с картиной до cb182717. Правая колонка в обоих замерах одинакова.
 *
 * Замер «до» (прод, превью, 1280px, 2026-09-13):
 *   rose/bloom — колонка красилась, но цветом ЖЁСТКО зашитой `color-scheme-2`
 *                (у bloom — вообще литералами в разметке страницы);
 *   vanilla/satin/flux — поверхности не было: центрированная сетка
 *                `max-w-[1280px]`, колонка обрывалась на 1079px из 1280 —
 *                справа оставалась полоса фона страницы.
 *
 * Раньше эта разметка жила ШЕСТЬЮ копиями (пять `themes/<t>/src/pages/
 * checkout.astro` + `preview.service.ts:wrapCheckoutGrid`), и в каждой копии
 * стоял комментарий «правки дублировать там же». Дублировали не всегда —
 * отсюда три темы без поверхности. Теперь копия одна.
 *
 * Контракт разметки:
 *   [data-checkout-pane="form"|"summary"]  — КОЛОНКА-поверхность. На SUMMARY
 *       сборка вешает `color-scheme-N` секции «Сводка заказа»
 *       (chrome-assembler: patchCheckoutColumnScheme), отсюда она и красится.
 *       На FORM класса схемы НЕТ — она остаётся на палитре страницы (фон
 *       темы).
 *   [data-checkout-column="form"|"summary"] — внутренний контент колонки.
 *       Имя сохранено: по нему липнет сводка (баг 17) и его же ищет
 *       `closest()` в инлайн-скрипте высоты (баг 18-Б).
 *   [data-checkout-slot="header"] внутри колонки формы — ШАПКА ОФОРМЛЕНИЯ.
 *       Эталон владельца: шапки НАД колонками нет, логотип стоит внутри левой
 *       колонки, поэтому правая идёт поверхностью от самого верха окна.
 *       Замер «до» (собранные витрины, Chromium, 1440×900, 2026-09-13): полоса
 *       шапки 0..134.39 (rose) / 0..132 (vanilla, flux, satin, bloom) над
 *       ОБЕИМИ колонками — белая полоса над цветной сводкой, её владелец и
 *       обвёл. Сборка подменяет эту шапку мерчантской по регулярке
 *       (chrome-assembler.injectChromeIntoHtml), а регулярка ищет `<header
 *       data-checkout-slot="header">` где угодно в документе — поэтому переезд
 *       внутрь колонки ей безразличен.
 */

/** Ширины и отступы — Figma 1:19998 (rose 1920: form 446, summary 556). */
export const CHECKOUT_SPLIT_CSS = `
.mfy-checkout-split { display: flex; flex-direction: column; width: 100%; min-height: 100dvh; background: rgb(var(--color-bg, 255 255 255)); }
.mfy-checkout-pane { width: 100%; box-sizing: border-box; color: rgb(var(--color-text, 0 0 0)); }
.mfy-checkout-pane__inner { width: 100%; max-width: 540px; margin: 0 auto; padding: 32px 16px; box-sizing: border-box; }
/* Класса схемы на этой колонке не бывает (см. шапку файла) → --color-bg
   берётся у палитры страницы: это фон ТЕМЫ, как до cb182717. Замер: четыре
   темы 255,255,255, vanilla 58,69,48 — при любой схеме формы. */
[data-checkout-pane="form"] { background: rgb(var(--color-bg, 255 255 255)); }
[data-checkout-pane="summary"] { background: rgb(var(--color-surface, 245 245 245)); }
/* Критерий владельца: «текст и вспомогательные элементы берут цвета из той же
   схемы». Наследование покрывает почти всё, но <button> по умолчанию берёт цвет
   у браузера (buttontext), а не у родителя: кнопка «Убрать ×» у промокода
   оставалась ЧЁРНОЙ на любой схеме — на тёмной поверхности её просто не видно
   (замер по пяти темам, 2026-09-13). Класс в блоке есть, но собранный CSS темы
   arbitrary-классы theme-base не печатает — чекаут красится правилами по
   data-атрибутам, поэтому и правило здесь, рядом с поверхностью колонки. */
[data-checkout-pane] [data-checkout-promo-remove] { color: rgb(var(--color-muted, 153 153 153)); background: transparent; }
[data-checkout-pane] [data-checkout-promo-remove]:hover { color: rgb(var(--color-text, 0 0 0)); }
/* ── Шапка оформления ВНУТРИ колонки формы ────────────────────────────────
   Раньше она шла полосой во всю ширину НАД обеими колонками, и над цветной
   сводкой оставалась белая полоса (замер: 132-134px, все пять тем). Внутри
   колонки шапка обязана вести себя как её первый блок, а не как полоса:
     • не липнуть — иначе логотип висит над полями формы (и над липкой сводкой
       напротив); липкость остаётся ТОЛЬКО у сводки (баги 17/18-Б);
     • не держать свой контейнер — у шапки это "mx-auto max-w-[…] px-4 md:px-8",
       из-за него логотип уезжал бы внутрь ещё на 16-32px и не совпадал бы с
       левым краем формы под ним (просьба владельца: «в тех же отступах»);
     • размеры логотипа (--size-checkout-brand*) раньше задавала обёртка в
       Layout темы — теперь они здесь, одной копией на пять тем.
     • не красить свою поверхность и не брать свою палитру — см. ниже.
   ПАЛИТРА ШАПКИ = ПАЛИТРА КОЛОНКИ. Пока шапка была отдельной полосой во всю
   ширину, своя «Цветовая схема» имела смысл. Внутри колонки — нет: замер
   (rose, 1440×900, колонка формы scheme-4, шапка scheme-2 из сида rose) дал
   белый прямоугольник 298..692 × 0..134 поверх чёрной колонки — то самое
   «пятно», от которого ушли в прошлом круге, только этажом ниже. Поэтому фон
   шапки прозрачный, а цвет логотипа и иконки берётся из переменной, ВЫЧИСЛЕННОЙ
   НА КОЛОНКЕ (--checkout-pane-heading): значение var() подставляется в момент
   объявления, то есть на колонке, и собственный класс схемы шапки на него уже
   не влияет. Так логотип читается на любой поверхности.
   ПОСЛЕДСТВИЕ, ЧЕСТНО: контрол «Цветовая схема» у секции «Шапка оформления»
   на странице чекаута больше ничего не меняет. Из панели он НЕ убран (состав
   параметров — канон), но его выбор перекрывается палитрой колонки. */
[data-checkout-pane] { --checkout-pane-heading: var(--color-heading, 0 0 0); }
[data-checkout-pane] [data-checkout-slot="header"] {
  position: static; width: 100%; z-index: auto; background: transparent;
  color: rgb(var(--checkout-pane-heading));
  --size-checkout-brand: 24px; --size-checkout-brand-image: 32px;
}
[data-checkout-pane] [data-checkout-slot="header"] a,
[data-checkout-pane] [data-checkout-slot="header"] svg {
  color: rgb(var(--checkout-pane-heading));
}
[data-checkout-pane] [data-checkout-slot="header"] > div {
  margin-left: 0; margin-right: 0; max-width: none; padding-left: 0; padding-right: 0;
}
/* Верхний отступ колонки задавала сама колонка (64px десктоп / 32px мобилка) —
   он существовал, чтобы форма не липла к полосе шапки. Шапка теперь ВНУТРИ, и
   верхний отступ даёт она сама (её параметр «Отступы»), иначе они сложились бы
   и логотип уехал бы на 112px от верха. */
.mfy-checkout-pane__inner--brand { padding-top: 0; }
/* ── Секция «Оформление заказа» = ВСЯ ЛЕВАЯ КОЛОНКА ───────────────────────
   Просьба владельца 14.09: «Оформление заказа секция идет все левое
   пространство до кнопки оплаты». Замер «до» (собранные витрины пяти тем,
   Chromium, 1440×900, корзина 3 позиции): колонка формы 0..720, а сама секция
   298..692 — полоска 394px по центру колонки. То есть секция была КАРТОЧКОЙ
   внутри колонки, ровно то, что канон чекаута запрещает («поверхность от края
   до края и на всю высоту, а не карточка внутри»), только про границы секции,
   а не про заливку.
   Причина была в том, ЧТО держит меру: её держала ОБЁРТКА колонки
   (max-width 446px + прижатие вправо), поэтому секция физически не могла быть
   шире 446. Мера переезжает на СОДЕРЖИМОЕ — на шапку оформления и на
   под-секции формы. Колонка перестаёт сужать, секция занимает её целиком, а
   поля внутри остаются на прежних 394px (Figma 1:19998) — замер «после»
   подтверждает: контакты/доставка/оплата/кнопка как были 298..692.
   Вертикаль не трогаем: секция и до правки занимала всю высоту колонки под
   шапкой (rose: 134 + 1179 + 64 = 1377 = высота колонки), кнопка оплаты внутри
   неё. Верхний/нижний отступы остаются на колонке — на корне секции их держать
   нельзя: мега-блок «Оформление заказа» печатает padding-top/bottom ИНЛАЙНОМ
   из своего (скрытого) параметра «Отступы», и инлайн перебил бы правило
   отсюда. */
[data-checkout-pane="form"] .mfy-checkout-pane__inner { max-width: none; margin: 0; padding-left: 0; padding-right: 0; }
[data-checkout-pane="form"] [data-checkout-slot="header"],
[data-checkout-pane="form"] [data-block="checkout-form"] > * {
  width: 100%; max-width: 540px; margin-left: auto; margin-right: auto;
  padding-left: 16px; padding-right: 16px; box-sizing: border-box;
}
@media (min-width: 1024px) {
  .mfy-checkout-split { flex-direction: row; align-items: stretch; }
  .mfy-checkout-pane { width: 50%; display: flex; }
  [data-checkout-pane="form"] { justify-content: flex-end; }
  [data-checkout-pane="summary"] { justify-content: flex-start; }
  [data-checkout-pane="form"] .mfy-checkout-pane__inner { margin: 0; max-width: none; padding: 64px 0; }
  /* Мера контента левой колонки на десктопе. Числа те же, что раньше стояли на
     обёртке (446 + 24/28), поэтому поля не сдвинулись ни на пиксель — сдвинулись
     ТОЛЬКО границы секции. */
  [data-checkout-pane="form"] [data-checkout-slot="header"],
  [data-checkout-pane="form"] [data-block="checkout-form"] > * {
    max-width: 446px; margin-left: auto; margin-right: 0;
    padding-left: 24px; padding-right: 28px;
  }
  [data-checkout-pane="summary"] .mfy-checkout-pane__inner { margin: 0 auto 0 0; max-width: 556px; padding: 64px 40px 64px 48px; }
  /* Тот же ноль, что и на мобилке, но повторён ВНУТРИ медиазапроса: строка выше
     задаёт padding СОКРАЩЁННО и с большей специфичностью (0,2,0 против 0,1,0),
     поэтому базовое правило --brand она перекрывала. Замер после первой правки:
     на 1440 логотип стоял на 112px от верха (64 колонки + 24 + 24 шапки) вместо
     48 — колонка добавляла свой отступ поверх отступа шапки. */
  [data-checkout-pane="form"] .mfy-checkout-pane__inner--brand { padding-top: 0; }
  /* Баг-репорт 17: сводка едет вместе с формой. Стояла в самом верху растянутой
     колонки — первый оборот колеса уводил её за кромку, и правая половина экрана
     превращалась в пустое неподвижное полотно. \`align-self\` обязателен:
     растянутой на всю высоту колонке прилипать негде.
     Баг-репорт 18-Б: прежняя страховка (\`max-height\` + свой скролл) колонку
     РЕЗАЛА — на 30 позициях прокрутка страницы не доставала до последних (видно
     9 из 30). Колонку не режем: сводка ниже экрана липнет низом (отрицательный
     \`--checkout-summary-top\`, считает блок «Сводка заказа») и едет со страницей
     до последней позиции. */
  [data-checkout-column="summary"] { position: sticky; top: var(--checkout-summary-top, 24px); align-self: start; }
}
`.trim();

/**
 * Та же разметка строкой — для превью конструктора, где блоки уже отрендерены
 * в HTML (`preview.service.wrapCheckoutGrid`). Классы схемы сюда не вшиваются:
 * их вешает общая доводка чекаута, одна и для витрины, и для превью.
 *
 * `headerHtml` — шапка оформления. Пусто → колонка без шапки (страховка для
 * старых ревизий без блока «Шапка оформления»): разметка тогда ровно прежняя.
 */
export function checkoutSplitMarkup(
  formHtml: string,
  summaryHtml: string,
  headerHtml = '',
): string {
  const brand = headerHtml ? ' mfy-checkout-pane__inner--brand' : '';
  return (
    `<style>${CHECKOUT_SPLIT_CSS}</style>` +
    '<div class="mfy-checkout-split">' +
    '<div class="mfy-checkout-pane mfy-checkout-pane--form" data-checkout-pane="form">' +
    `<div class="mfy-checkout-pane__inner${brand}" data-checkout-column="form" style="min-width:0">` +
    headerHtml +
    formHtml +
    '</div></div>' +
    '<div class="mfy-checkout-pane mfy-checkout-pane--summary" data-checkout-pane="summary">' +
    '<div class="mfy-checkout-pane__inner" data-checkout-column="summary" style="min-width:0">' +
    summaryHtml +
    '</div></div>' +
    '</div>'
  );
}
