---
import '../styles/global.css';

interface Props {
  title: string;
  description: string;
  locale?: string;               // default 'ru'
  canonicalUrl?: string;
  ogImage?: string;
  noIndex?: boolean;
  bodyClass?: string;
  tokensCss?: string;            // injected tokens.css
  fontHead?: string;             // injected font <link>s
}

const {
  title,
  description,
  locale = 'ru',
  canonicalUrl,
  ogImage,
  noIndex = false,
  bodyClass = '',
  tokensCss = '',
  fontHead = '',
} = Astro.props as Props;
---
<!DOCTYPE html>
<html lang={locale}>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{title}</title>
  <meta name="description" content={description}>
  {canonicalUrl && <link rel="canonical" href={canonicalUrl}>}
  {noIndex && <meta name="robots" content="noindex, nofollow">}

  {/* Google Fonts — default theme fonts; scaffold-builder step 7b replaces
     this href when merchant customises fonts via themeSettings. */}
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Comfortaa:wght@300..700&family=Manrope:wght@200..800&display=swap" rel="stylesheet" />

  {/* Font preload injection point (constructor preview overrides) */}
  <Fragment set:html={fontHead} />

  {/* Inline token overrides from preview/constructor (may be empty in live builds) */}
  {tokensCss && <style set:html={tokensCss}></style>}

  {/* Open Graph */}
  <meta property="og:type" content="website">
  <meta property="og:title" content={title}>
  <meta property="og:description" content={description}>
  {canonicalUrl && <meta property="og:url" content={canonicalUrl}>}
  {ogImage && <meta property="og:image" content={ogImage}>}

  {/* Twitter Card */}
  <meta name="twitter:card" content={ogImage ? 'summary_large_image' : 'summary'}>
  <meta name="twitter:title" content={title}>
  <meta name="twitter:description" content={description}>
  {ogImage && <meta name="twitter:image" content={ogImage}>}

  {/* Tracking scripts slot — TrackingScripts.astro injects here */}
  <slot name="head" />
</head>
<body class={bodyClass}>
  <slot />
  <slot name="body-end" />
</body>
</html>
