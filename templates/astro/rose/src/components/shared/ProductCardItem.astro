---
interface Props {
	id?: string;
	name: string;
	price: string;
	oldPrice?: string;
	image: string;
	href?: string;
	quantity?: number;
}

const { id, name, price, oldPrice, image, href = '#', quantity } = Astro.props;
const hasImage = image && !image.includes('placeholder');
---

<div class="flex flex-col gap-4 sm:gap-5 md:gap-6 lg:gap-[25px] group">
	<a href={href} class="cursor-pointer">
		<div class="bg-gray-100 rounded-lg sm:rounded-[8px] md:rounded-[10px] overflow-hidden w-full aspect-[318/515]">
			{hasImage ? (
				<img
					src={image}
					alt={name}
					loading="lazy"
					class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
				/>
			) : (
				<div class="w-full h-full flex items-center justify-center bg-gray-100">
					<svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round" class="text-gray-300">
						<rect x="3" y="3" width="18" height="18" rx="2" ry="2" />
						<circle cx="8.5" cy="8.5" r="1.5" />
						<polyline points="21 15 16 10 5 21" />
					</svg>
				</div>
			)}
		</div>
	</a>
	<div class="flex flex-col gap-2 sm:gap-2.5 md:gap-3 lg:gap-[10px] px-2 sm:px-3 md:px-4 lg:px-[15px]">
		<a href={href} class="no-underline text-inherit">
			<h3 class="text-base sm:text-lg md:text-xl lg:text-[24px] font-normal text-theme-foreground leading-[1.366] font-body line-clamp-2">
				{name}
			</h3>
		</a>
		<div class="flex items-center gap-2 sm:gap-3 md:gap-4 lg:gap-[15px] flex-wrap">
			<span class="text-lg sm:text-xl md:text-2xl lg:text-[32px] font-normal text-theme-foreground leading-[1.366] font-body">
				{price}
			</span>
			{oldPrice && (
				<span class="text-sm sm:text-base md:text-lg lg:text-[20px] font-medium text-theme-muted line-through leading-[1.366] font-body">
					{oldPrice}
				</span>
			)}
		</div>
		{id && (
			<button
				type="button"
				class="add-to-cart-btn mt-2 py-2.5 px-5 bg-theme-primary text-white text-sm font-medium rounded-lg hover:opacity-90 transition-colors"
				data-product-id={id}
				data-stock={quantity != null ? quantity : undefined}
			>
				В корзину
			</button>
		)}
	</div>
</div>
