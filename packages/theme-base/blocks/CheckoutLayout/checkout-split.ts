/**
 * Split-чекаут: ОДИН источник разметки и стилей для пяти тем и для превью.
 *
 * Страница состоит из трёх узлов дерева конструктора, и у каждого — своя роль
 * и своя «Цветовая схема» (четвёртый круг тестировщика, 14.09):
 *
 *   ШАПКА ОФОРМЛЕНИЯ — отдельная полоса ВО ВСЮ ШИРИНУ над обеими колонками.
 *       Дословно: «секция шапка неправильно отображена. Ожидаемый результат:
 *       отделить от левой и правой части и придать свои размеры». Третий круг
 *       уводил её ВНУТРЬ левой колонки (чтобы над цветной сводкой не висела
 *       белая полоса) — это решение отменено: полоса не белая, она несёт свою
 *       схему. Замер «до» (собранные витрины пяти тем, Chromium, 1440×900,
 *       14.09): шапка x=274 w=446 — внутри колонки формы (0..720).
 *
 *   ОФОРМЛЕНИЕ ЗАКАЗА — вся ЛЕВАЯ колонка. Границы секции сюда приехали в
 *       прошлом круге (мера с обёртки колонки переехала на содержимое), а в
 *       этом к ним вернулась ПОКРАСКА: «к секции Оформление заказа не
 *       применяется никакая цветовая схема». Сужение «из схемы формы только
 *       кнопка» держалось на том, что секция была карточкой 394px по центру
 *       колонки и класс схемы красил «пятно»; карточки больше нет, поэтому
 *       схема красит колонку — как записано в каноне чекаута («поверхность от
 *       края до края и на всю высоту, а не карточка внутри»).
 *
 *   СВОДКА ЗАКАЗА — вся ПРАВАЯ колонка. «Секция Сводка заказа отображается не
 *       в полный размер всей правой части». Замер «до»: колонка x=720 w=720
 *       h≈1276-1298, секция x=768 w=468 h≈462-464 — карточка в 65 % ширины и
 *       36 % высоты колонки. Мера переезжает на СОДЕРЖИМОЕ секции теми же
 *       числами (556 + 48/40 десктоп, 540 + 16 мобилка), поэтому позиции и
 *       итоги не сдвигаются — сдвигаются только границы секции.
 *
 *   ПОДВАЛ — юр.инфа (блок условий под кнопкой). Схему на него вешает
 *       `chrome-assembler` (`patchCheckoutBlockScheme(html,'checkout-terms')`),
 *       здесь для него правил нет.
 *
 * ПОЧЕМУ ПРАВАЯ КОЛОНКА БОЛЬШЕ НЕ НЕСЁТ СВОЕЙ ПОВЕРХНОСТИ (16.09). Владелец
 * прислал скриншот Схемы 1 (Фон #000000, Заголовок #FFFFFF, Текст #FFFFFF) и
 * написал дословно: «цвет тот же, схема та же, но они различаются» — левая и
 * правая колонки обе тёмные, но граница между ними видна.
 *
 * История вопроса (для контекста, поведение уже не действует). Четвёртый круг
 * (14.09): «для секции Сводка заказа применяются только заданые цветовые схемы
 * от тем, а при измении их не принимает новые условия» — колонка красилась
 * `--color-surface`, поля `surfaceBg` в редакторе схем нет, мерчант его
 * изменить не мог. Лечили отдельным токеном `--color-checkout-surface`
 * (`checkoutSurfaceOf` в `tokens-css`): сначала выборочно (15.09 поправка —
 * тотально, у каждой схемы), потом с приоритетом «осознанно заданная
 * поверхность → перекрашенный Фон → заводская поверхность схемы». Итог замера
 * «до» на сидовых данных (16.09): поверхность отличалась от Фона на rose (все
 * 5 схем), satin (2 из 4), flux (все 4) — на 5-30 единиц RGB, отсюда и видимая
 * граница со скриншота владельца.
 *
 * РЕШЕНИЕ 16.09. Токен `--color-checkout-surface` снят — концепция отдельной
 * поверхности правой колонки больше не применяется к ней вовсе. Колонка
 * красится ТЕМ ЖЕ `--color-bg`, что и левая (строка ниже дословно повторяет
 * `[data-checkout-pane="form"]`). Существующие магазины ВИД МЕНЯЮТ: у rose
 * (сид пинит `scheme-2`, 245,245,245 → 255,255,255) и flux правая колонка
 * посветлеет/потемнеет до цвета левой — это и есть цель правки, а не
 * побочный эффект.
 *
 * Раньше эта разметка жила ШЕСТЬЮ копиями (пять `themes/<t>/src/pages/
 * checkout.astro` + `preview.service.ts:wrapCheckoutGrid`), и в каждой копии
 * стоял комментарий «правки дублировать там же». Дублировали не всегда —
 * отсюда три темы без поверхности. Теперь копия одна.
 *
 * Контракт разметки:
 *   [data-checkout-topbar]                 — полоса шапки оформления, первый
 *       узел сплита, НАД строкой колонок и во всю её ширину.
 *   [data-checkout-cols]                   — строка колонок (она делит экран
 *       пополам на десктопе и складывается в столбец на мобилке).
 *   [data-checkout-pane="form"|"summary"]  — КОЛОНКА-поверхность. На неё
 *       сборка вешает `color-scheme-N` соответствующей секции
 *       (chrome-assembler: patchCheckoutColumnScheme).
 *   [data-checkout-column="form"|"summary"] — СОДЕРЖИМОЕ колонки, оно держит
 *       меру. У сводки этот узел переехал ВНУТРЬ секции (иначе секция не может
 *       быть колонкой), поэтому по нему же липнет сводка (баг 17) и его же
 *       ищет инлайн-скрипт высоты (баг 18-Б). Вертикальные отступы 64/32 едут
 *       ВМЕСТЕ с ним: они и раньше лежали на липком элементе (обёртке колонки),
 *       поэтому прилипшая сводка стоит там же, где стояла, — а секция при этом
 *       занимает колонку ЦЕЛИКОМ, без полос сверху и снизу.
 */

/** Ширины и отступы — Figma 1:19998 (rose 1920: form 446, summary 556). */
export const CHECKOUT_SPLIT_CSS = `
.mfy-checkout-split { display: flex; flex-direction: column; width: 100%; min-height: 100dvh; background: rgb(var(--color-bg, 255 255 255)); }
/* ── Шапка оформления: СВОЯ полоса над обеими колонками ────────────────────
   «Отделить от левой и правой части и придать свои размеры». Полоса во всю
   ширину, вертикальные отступы берёт из собственного параметра «Отступы»
   (блок печатает их инлайном), контейнер — свой, из разметки блока. Своя
   «Цветовая схема» полосы снова работает: она больше не внутри колонки, и
   перекрывать её палитрой колонки не нужно. */
.mfy-checkout-topbar { width: 100%; flex: none; }
.mfy-checkout-topbar [data-checkout-slot="header"] { position: static; width: 100%; --size-checkout-brand: 24px; --size-checkout-brand-image: 32px; }
.mfy-checkout-cols { display: flex; flex-direction: column; width: 100%; flex: 1 1 auto; }
.mfy-checkout-pane { width: 100%; box-sizing: border-box; color: rgb(var(--color-text, 0 0 0)); }
.mfy-checkout-pane__inner { width: 100%; max-width: 540px; margin: 0 auto; padding: 32px 16px; box-sizing: border-box; }
/* Схема секции «Оформление заказа» садится на эту колонку — тогда
   \`--color-bg\` берётся у схемы; без схемы это фон ТЕМЫ, как и раньше. */
[data-checkout-pane="form"] { background: rgb(var(--color-bg, 255 255 255)); }
/* Поверхность правой колонки — 16.09: убрана. Владелец прислал скриншот и
   сказал дословно «цвет тот же, схема та же, но они различаются» (Схема 1
   rose: Фон #000000, обе колонки тёмные, но граница видна). До этой правки
   колонка красилась отдельным \`--color-checkout-surface\` (считал
   \`checkoutSurfaceOf\` в tokens-css) — заводская поверхность темы отличалась
   от «Фона» на 21-26 единиц почти у всех тем (rose/satin/flux, замер «до» —
   см. checkout-summary-scheme-surface.spec.ts). Теперь колонка красится
   ТЕМ ЖЕ \`--color-bg\`, что и левая — строка идентична правилу
   \`[data-checkout-pane="form"]\` выше. Понятие отдельной поверхности чекаута
   снято: \`--color-checkout-surface\` больше не считается и не читается —
   ни одно CSS-правило пакета на него не ссылается. */
[data-checkout-pane="summary"] { background: rgb(var(--color-bg, 255 255 255)); }
/* Критерий владельца: «текст и вспомогательные элементы берут цвета из той же
   схемы». Наследование покрывает почти всё, но <button> по умолчанию берёт цвет
   у браузера (buttontext), а не у родителя: кнопка «Убрать ×» у промокода
   оставалась ЧЁРНОЙ на любой схеме — на тёмной поверхности её просто не видно
   (замер по пяти темам, 2026-09-13). Класс в блоке есть, но собранный CSS темы
   arbitrary-классы theme-base не печатает — чекаут красится правилами по
   data-атрибутам, поэтому и правило здесь, рядом с поверхностью колонки. */
[data-checkout-pane] [data-checkout-promo-remove] { color: rgb(var(--color-muted, 153 153 153)); background: transparent; }
[data-checkout-pane] [data-checkout-promo-remove]:hover { color: rgb(var(--color-text, 0 0 0)); }
/* ── Секция «Оформление заказа» = ВСЯ ЛЕВАЯ КОЛОНКА ───────────────────────
   Прошлый круг: «Оформление заказа секция идет все левое пространство до
   кнопки оплаты». Замер «до»: колонка формы 0..720, секция 298..692 — полоска
   394px по центру. Меру держала ОБЁРТКА колонки (max-width 446px + прижатие
   вправо), поэтому секция физически не могла быть шире. Мера переехала на
   СОДЕРЖИМОЕ — на под-секции формы. Поля остались на прежних 394px
   (Figma 1:19998). Вертикаль не трогаем: верхний/нижний отступы остаются на
   колонке — на корне секции их держать нельзя, мега-блок печатает
   padding-top/bottom ИНЛАЙНОМ из своего параметра «Отступы». */
[data-checkout-pane="form"] .mfy-checkout-pane__inner { max-width: none; margin: 0; padding-left: 0; padding-right: 0; }
[data-checkout-pane="form"] [data-block="checkout-form"] > * {
  width: 100%; max-width: 540px; margin-left: auto; margin-right: auto;
  padding-left: 16px; padding-right: 16px; box-sizing: border-box;
}
/* ── Секция «Сводка заказа» = ВСЯ ПРАВАЯ КОЛОНКА ──────────────────────────
   Зеркало левой колонки, тот же приём: обёртка перестаёт держать меру, секция
   занимает колонку целиком (по ширине — обёрткой, по высоте — ростом внутри
   flex-столбца), а меру держит СОДЕРЖИМОЕ секции. Липкость сводки (баги 17 и
   18-Б) переезжает на это же содержимое: секция теперь во всю высоту колонки,
   и липнуть внутри неё есть куда. */
[data-checkout-pane="summary"] .mfy-checkout-pane__inner { display: flex; flex-direction: column; max-width: none; margin: 0; padding: 0; }
[data-checkout-pane="summary"] [data-block="checkout-summary"] { width: 100%; flex: 1 1 auto; }
[data-checkout-pane="summary"] [data-checkout-column="summary"] {
  width: 100%; max-width: 540px; margin-left: auto; margin-right: auto;
  padding: 32px 16px; box-sizing: border-box;
  display: flex; flex-direction: column; gap: 24px;
}
@media (min-width: 1024px) {
  .mfy-checkout-cols { flex-direction: row; align-items: stretch; }
  .mfy-checkout-pane { width: 50%; display: flex; }
  [data-checkout-pane="form"] { justify-content: flex-end; }
  [data-checkout-pane="summary"] { justify-content: flex-start; }
  [data-checkout-pane="form"] .mfy-checkout-pane__inner { margin: 0; max-width: none; padding: 64px 0; }
  /* Мера контента левой колонки на десктопе. Числа те же, что раньше стояли на
     обёртке (446 + 24/28), поэтому поля не сдвинулись ни на пиксель. */
  [data-checkout-pane="form"] [data-block="checkout-form"] > * {
    max-width: 446px; margin-left: auto; margin-right: 0;
    padding-left: 24px; padding-right: 28px;
  }
  [data-checkout-pane="summary"] .mfy-checkout-pane__inner { display: flex; flex-direction: column; margin: 0; max-width: none; padding: 0; }
  /* Мера контента правой колонки — прежние 556 + 48/40 с обёртки: позиции,
     промокод и итоги стоят там же, где стояли (замер: x=768 w=468).
     Баг-репорт 17: сводка едет вместе с формой. Баг-репорт 18-Б: колонку не
     режем (\`max-height\` + свой скролл прятали последние позиции) — сводка ниже
     экрана липнет НИЗОМ через отрицательный \`--checkout-summary-top\`, который
     считает инлайн-скрипт блока «Сводка заказа». */
  [data-checkout-pane="summary"] [data-checkout-column="summary"] {
    max-width: 556px; margin-left: 0; margin-right: auto;
    padding: 64px 40px 64px 48px;
    position: sticky; top: var(--checkout-summary-top, 24px);
  }
}
`.trim();

/**
 * Та же разметка строкой — для превью конструктора, где блоки уже отрендерены
 * в HTML (`preview.service.wrapCheckoutGrid`). Классы схемы сюда не вшиваются:
 * их вешает общая доводка чекаута, одна и для витрины, и для превью.
 *
 * `headerHtml` — шапка оформления, ОТДЕЛЬНАЯ полоса над строкой колонок. Пусто
 * → полосы нет (страховка для старых ревизий без блока «Шапка оформления»).
 */
export function checkoutSplitMarkup(
  formHtml: string,
  summaryHtml: string,
  headerHtml = '',
): string {
  return (
    `<style>${CHECKOUT_SPLIT_CSS}</style>` +
    '<div class="mfy-checkout-split">' +
    (headerHtml
      ? `<div class="mfy-checkout-topbar" data-checkout-topbar>${headerHtml}</div>`
      : '') +
    '<div class="mfy-checkout-cols" data-checkout-cols>' +
    '<div class="mfy-checkout-pane mfy-checkout-pane--form" data-checkout-pane="form">' +
    '<div class="mfy-checkout-pane__inner" data-checkout-column="form" style="min-width:0">' +
    formHtml +
    '</div></div>' +
    '<div class="mfy-checkout-pane mfy-checkout-pane--summary" data-checkout-pane="summary">' +
    '<div class="mfy-checkout-pane__inner" style="min-width:0">' +
    summaryHtml +
    '</div></div>' +
    '</div></div>'
  );
}
