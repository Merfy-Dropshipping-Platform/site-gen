NODE_ENV=development
PORT=3114
RABBITMQ_URL=amqp://rabbitmq:password@localhost:5672
DATABASE_URL=postgresql://postgres:postgres123@localhost:5432/sites_service

# Coolify (optional; set COOLIFY_MODE=http to enable HTTP calls)
COOLIFY_MODE=mock
COOLIFY_API_URL=http://localhost:8000
COOLIFY_API_TOKEN=
COOLIFY_API_PREFIX=/v1
# При необходимости выровнять эндпоинты под фактический Coolify API:
# COOLIFY_ENDPOINT_ENSURE=/v1/apps/ensure
# COOLIFY_ENDPOINT_DEPLOY=/v1/apps/deploy
# COOLIFY_ENDPOINT_SET_DOMAIN=/v1/apps/domains
# COOLIFY_ENDPOINT_MAINTENANCE=/v1/apps/maintenance
# Включение/выключение Astro‑сборки (по умолчанию включена)
ASTRO_BUILD_ENABLED=true

# Optional S3/Minio settings for artifact uploads (not used yet; planned)
# S3_ENDPOINT=http://localhost:9000
# S3_REGION=us-east-1
# S3_ACCESS_KEY=minioadmin
# S3_SECRET_KEY=minioadmin
# S3_BUCKET=merfy-sites
# S3_PUBLIC_ENDPOINT=http://localhost:9000

# Retention for local artifacts in days
ARTIFACT_RETENTION_DAYS=14

# Enable/disable hourly billing reconciliation job (freeze/unfreeze fallback)
BILLING_SYNC_CRON_ENABLED=true
