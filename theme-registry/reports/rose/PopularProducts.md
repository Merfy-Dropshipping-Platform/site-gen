# PopularProducts @ rose — прогон реестра

Дата: 2026-08-06T18:39:12.777Z · site e03dd420-febf-4499-9fc4-992412cc1b99 · page home · blockId PopularProducts-1

| поле | вердикт | измерено | смысл (по Rose) |
|---|---|---|---|
| Карточки (`cards`) | ✓ | 4 → 4 плиток · 8 → 8 плиток | число карточек в сетке (Popular.astro:40 — placeholderCards из cardsN; реальные — срез коллекции) |
| Заголовок (`heading`) | ✓ | «Товары проба Альфа» видим · «Товары проба Бета» видим | введённый заголовок секции видим |
| Размер заголовка (`headingSize`) | ✓ | кегль 20 < 24 < 29px | кегль заголовка растёт ([&_h2]-утилиты, Popular.astro:50-55; top-level приоритетнее legacy — cc64b373) |
| Текст (`text`) | ✓ | «Описание товаров Альфа» видим · «Описание товаров Бета» видим | введённый подзаголовок видим |
| Размер текста (`textSize`) | ✓ | кегль 16 < 19 < 23px | кегль подзаголовка растёт |
| Колонки (`columns`) | ✓ | плитка 610px (2 кол., в ряду 2) → 295px (4 кол., в ряду 4) | число колонок сетки (--cols, Popular.astro:68) — плитки сужаются пропорционально |
| Отступы (`padding`) | ✓ | высота +160px при +160px отступов | внутренние отступы секции |

## Не покрыто

- `collection` — выбор коллекции = данные тенанта (у rose-гейта нет коллекций); волна данных
- `buttonStyle` — стиль кнопки живёт на РЕАЛЬНЫХ карточках — плейсхолдеры сознательно без кнопок; волна данных
- `imageView` — аспект применяется селектором li>article>a (реальные карточки); плейсхолдер — article>div с фикс-аспектом 318/444; волна данных
- `nextPhotoOnHover` — второе фото при наведении = интерактив + нужен товар с ≥2 фото; волна данных/интерактива
- `quickAddMode` — кнопка быстрого добавления только на реальных карточках; волна данных
- `colorScheme` — схему вешает компоновщик страницы — page-tier волна
