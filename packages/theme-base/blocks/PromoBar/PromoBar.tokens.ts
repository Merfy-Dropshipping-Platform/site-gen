// PromoBar uses --color-bg-promo + --color-text-promo CSS vars (defaults to
// black/white). Themes override via theme.json defaults if needed.
export const PromoBarTokens = [
  '--color-bg-promo',
  '--color-text-promo',
] as const;
