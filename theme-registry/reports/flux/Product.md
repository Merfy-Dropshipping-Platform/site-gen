# Product @ flux — прогон реестра

Дата: 2026-09-08T20:48:35.753Z · site 132d3a3e-a28f-40b7-98fa-a0200151cfb8 · page home · blockId Product-home

| поле | вердикт | измерено | смысл (по Rose) |
|---|---|---|---|
| Размер (`size`) | ✓ | медиа 408 < 475 < 497px | ширина колонки главного фото: 440/552/640 (--product-main-image-size, Figma 648:55733) |
| Позиция фото (`photoPosition`) | ✗ | left: центр медиа 28% · right: центр медиа 28% ✗ | главное фото слева ↔ справа от инфо-колонки |
| Текст · содержимое (`text.content`) | ✗ | «Текст товара проба Альфа» НЕ ВИДИМ · «Текст товара проба Бета» НЕ ВИДИМ | введённый текст секции видим |
| Текст · размер (`text.size`) | ✗ | текст «Проба кегля текста товара» не найден/невидим | кегль текста секции растёт |
| Название · размер (`title.size`) | ✗ | кегль 24 < 24 < 24px | кегль названия товара растёт |
| Поделиться · текст (`share.text`) | ✗ | «Поделиться проба Альфа» НЕ ВИДИМ · «Поделиться проба Бета» НЕ ВИДИМ | подпись кнопки «Поделиться» видима |
| Отступы (`padding`) | ✗ | высота +0px при +160px отступов | внутренние отступы секции сверху/снизу |
| Выбор товара (`productId`) | ✗ | «Наушники Реестр Про» → «Наушники Реестр Про» | блок рендерит ВЫБРАННЫЙ товар тенанта (имя/цена/галерея меняются) |
| Макет (`layout`) | ✗ | carousel: крупных=1, h=746px · two-columns: крупных=1, h=746px | раскладка ГАЛЕРЕИ фото: carousel = один кадр + тумбы, two-columns = плитка крупных |
| Вариации · стиль (`variants.displayStyle`) | ✓ | button: скрыт (ждали скрыт) · list: видим (ждали видим) | button = чипы-кнопки опций; list = выпадающие списки (select) |
| Вариации · форма (`variants.shape`) | ✓ | none: текст-чип · circle: свотч r=35711% · square: свотч r=0% | none = текст-чипы; circle/square = свотчи цвета соотв. формы (swatchHex опций) |
| Основная кнопка (`buttons.addToCart.text`) | ✓ | В корзину проба: видим (ждали видим) · : скрыт (ждали скрыт) | текст «Добавить в корзину»; пустая строка скрывает кнопку |

## Не покрыто

- `zoomMode` — клик/ховер-зум = интерактив (нужен реальный клик/ховер), вне волны 1 «только смена настроек»
- `dynamicButton` — плавающая кнопка проявляется при скролле = интерактив, вне волны 1
- `colorScheme` — схему вешает компоновщик страницы, а не блок — каналом update-block не меряется; page-tier волна
