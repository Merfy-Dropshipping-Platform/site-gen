---
import BaseLayout from '../layouts/BaseLayout.astro';
import data from '../data/data.json';
import siteConfig from '../data/site-config.json';

// Puck block components — render content from pagesData['page-catalog'].content[]
import Header from '../components/Header.astro';
import Hero from '../components/Hero.astro';
import Footer from '../components/Footer.astro';
import PopularProducts from '../components/PopularProducts.astro';
import Gallery from '../components/Gallery.astro';
import TextBlock from '../components/TextBlock.astro';
import MultiColumns from '../components/MultiColumns.astro';
import MultiRows from '../components/MultiRows.astro';
import Slideshow from '../components/Slideshow.astro';
import Video from '../components/Video.astro';
import ImageWithText from '../components/ImageWithText.astro';
import ContactForm from '../components/ContactForm.astro';
import PromoBanner from '../components/PromoBanner.astro';
import Publications from '../components/Publications.astro';
import CollapsibleSection from '../components/CollapsibleSection.astro';
import Collections from '../components/Collections.astro';
import Newsletter from '../components/Newsletter.astro';
import Product from '../components/Product.astro';
import Catalog from '../components/Catalog.astro';

// Resolve catalog page blocks from multipage revision shape
const allPagesData = (data as any)?.pagesData ?? {};
const catalogPage =
  allPagesData['page-catalog'] ??
  ((data as any)?.currentPageId === 'page-catalog' ? data : null);

const contentBlocks = catalogPage?.content
  ? (Array.isArray(catalogPage.content) ? catalogPage.content : [])
  : Array.isArray((data as any)?.content) && (data as any)?.currentPageId === 'page-catalog'
    ? (data as any).content
    : [];

const meta = data?.meta ?? {};
// Pass siteId (shopId) into Catalog so its inline fetch script has the API
// target without depending on window.__MERFY_CONFIG__ being available
// before the script runs. Mirrors how the preview controller injects siteId.
const siteId = (meta as any)?.shopId ?? (meta as any)?.siteId ?? '';

// Header/Footer fallbacks from siteConfig
const homePage = allPagesData['home'];
const homeBlocks: any[] = (homePage?.content && Array.isArray(homePage.content)) ? homePage.content : [];
const headerBlock = homeBlocks.find((b: any) => b.type === 'Header');
const footerBlock = homeBlocks.find((b: any) => b.type === 'Footer');
const headerProps = headerBlock?.props ?? (siteConfig as any)?.header ?? {};
const footerProps = footerBlock?.props ?? (siteConfig as any)?.footer ?? {};

const componentMap: Record<string, any> = {
  Hero, PopularProducts, Gallery, TextBlock, MultiColumns, MultiRows, Slideshow,
  Video, ImageWithText, ContactForm, PromoBanner, Publications,
  CollapsibleSection, Collections, Newsletter, Product,
  Catalog,
};

// If merchant hasn't customised the page (no content blocks), fall back to a
// default Catalog block so /catalog is functional out of the box.
// Otherwise we trust whatever blocks the merchant placed via the constructor
// (082 catalog parity — same behaviour as other route pages).
const hasMerchantContent = contentBlocks.length > 0;
const defaultBlocks = [
  {
    type: 'Catalog',
    props: {
      id: 'Catalog-default',
      cards: 24,
      columns: 3,
      showFilter: 'true',
      filterPosition: 'side',
      showSort: 'true',
      padding: { top: 40, bottom: 80 },
    },
  },
];
const renderBlocks = hasMerchantContent ? contentBlocks : defaultBlocks;
const pageTitle = (meta?.title && `Каталог — ${meta.title}`) || 'Каталог';
---
<BaseLayout title={pageTitle} description={meta?.description}>
  <Header {...headerProps} />

  <main class="flex-1 w-full">
    {renderBlocks.map((block: any) => {
      const Component = componentMap[block.type];
      if (!Component) return null;
      // Inject siteId into Catalog blocks so the inline fetch hydrates
      // placeholder cards with real products from storefront-data.
      const extraProps = block.type === 'Catalog' && siteId ? { siteId } : {};
      return <Component {...(block.props ?? {})} {...extraProps} />;
    })}
  </main>

  <Footer {...footerProps} />
</BaseLayout>
