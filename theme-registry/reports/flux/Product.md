# Product @ flux — прогон реестра

Дата: 2026-08-10T03:55:08.235Z · site 132d3a3e-a28f-40b7-98fa-a0200151cfb8 · page product · blockId Product-1

| поле | вердикт | измерено | смысл (по Rose) |
|---|---|---|---|
| Размер (`size`) | ✓ | медиа 520 < 611 < 657px | ширина колонки главного фото: 440/552/640 (--product-main-image-size, Figma 648:55733) |
| Позиция фото (`photoPosition`) | ✓ | left: центр медиа 28% · right: центр медиа 72% | главное фото слева ↔ справа от инфо-колонки |
| Текст · содержимое (`text.content`) | ✓ | «Текст товара проба Альфа» видим · «Текст товара проба Бета» видим | введённый текст секции видим |
| Текст · размер (`text.size`) | ✓ | кегль 14 < 16 < 18px | кегль текста секции растёт |
| Название · размер (`title.size`) | ✓ | кегль 20 < 24 < 29px | кегль названия товара растёт |
| Поделиться · текст (`share.text`) | ✓ | «Поделиться проба Альфа» видим · «Поделиться проба Бета» видим | подпись кнопки «Поделиться» видима |
| Отступы (`padding`) | ✓ | высота +160px при +160px отступов | внутренние отступы секции сверху/снизу |
| Выбор товара (`productId`) | ✓ | «M Headphones 2s» → «Наушники Реестр Про» | блок рендерит ВЫБРАННЫЙ товар тенанта (имя/цена/галерея меняются) |
| Макет (`layout`) | ✓ | carousel: крупных=1, h=875px · two-columns: крупных=3, h=739px | раскладка ГАЛЕРЕИ фото: carousel = один кадр + тумбы, two-columns = плитка крупных |
| Вариации · стиль (`variants.displayStyle`) | ✓ | button: скрыт (ждали скрыт) · list: видим (ждали видим) | button = чипы-кнопки опций; list = выпадающие списки (select) |
| Вариации · форма (`variants.shape`) | ✓ | none: текст-чип · circle: свотч r=35711% · square: свотч r=0% | none = текст-чипы; circle/square = свотчи цвета соотв. формы (swatchHex опций) |
| Основная кнопка (`buttons.addToCart.text`) | ✓ | В корзину проба: видим (ждали видим) · : скрыт (ждали скрыт) | текст «Добавить в корзину»; пустая строка скрывает кнопку |

## Не покрыто

- `zoomMode` — клик/ховер-зум = интерактив (нужен реальный клик/ховер), вне волны 1 «только смена настроек»
- `dynamicButton` — плавающая кнопка проявляется при скролле = интерактив, вне волны 1
- `colorScheme` — схему вешает компоновщик страницы, а не блок — каналом update-block не меряется; page-tier волна
