---
/**
 * Страница товара `/product` — ЕДИНЫЙ рендер (PRODUCT_UNIFIED_THEMES).
 *
 * Этот файл даёт только ШЕЛЛ страницы (Layout: <head>, шапка/подвал, скрипты
 * Layout). Тело секции «Товар» инжектит sites на сборке/в превью через
 * renderBlock(theme-base `Product.astro`) — composeContentPagesIntoDist (live)
 * и preview.controller (превью) заменяют <main> отрендеренной секцией с
 * настройками из page-product ревизии. Per-slug `/product/<slug>` собирается
 * тем же путём (build.service `renderProductSectionForId`).
 *
 * Порт `RoseProductDetail.astro` удалён: рендер PDP унифицирован с конструктором
 * и контентными секциями (один `Product.astro` везде — live, превью, главная).
 * Фолбэк при сбое compose — пустой шелл (compose — основной путь ВСЕХ контентных
 * страниц темы, надёжен).
 */
import Layout from "../layouts/Layout.astro";
---

<Layout title="Товар"></Layout>
