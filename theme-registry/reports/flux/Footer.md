# Footer @ flux — прогон реестра

Дата: 2026-08-06T19:32:34.407Z · site 132d3a3e-a28f-40b7-98fa-a0200151cfb8 · page home · blockId Footer-home

| поле | вердикт | измерено | смысл (по Rose) |
|---|---|---|---|
| Рассылка (`newsletter.enabled`) | ✓ | true: видим (ждали видим) · false: скрыт (ждали скрыт) | тумблер блока рассылки: форма e-mail появляется/уходит |
| Заголовок · текст (`heading.text`) | ✓ | «Подвал проба Альфа» видим · «Подвал проба Бета» видим | заголовок блока рассылки из панели (b71bd733 — поля живые, не хардкод) |
| Заголовок · размер (`heading.size`) | ✓ | кегль 18 < 20 < 24px | кегль заголовка рассылки растёт (roseFooterHeadingStyle → --size-section-heading) |
| Текст · содержимое (`text.content`) | ✓ | «Текст подвала Альфа» видим · «Текст подвала Бета» видим | текст блока рассылки из панели |
| Текст · размер (`text.size`) | ✓ | кегль 14 < 16 < 18px | кегль текста рассылки растёт |
| Цветовая схема (`colorScheme`) | ✓ | вид меняется: rgb(26, 26, 26) ↔ rgb(251, 251, 251) | схема на <footer> (54ccd4b1 — рендер сам вешает класс, меряем через re-render канала) |
| Отступы (`padding`) | ✓ | высота +160px при +160px отступов | внутренние отступы подвала |

## Не покрыто

- `heading.alignment` — рендер эталона НЕ читает сабполе (выравнивание живёт в скрытом top-level contentAlign, Footer.astro:70) — мёртвый сабфилд панели; кандидат: замапить или скрыть. Доложено.
