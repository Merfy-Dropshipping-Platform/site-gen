-- Дата, когда МЕРЧАНТ выбрал тему для своего магазина.
--
-- Баг владельца 20.09: на карточке темы в админке «Добавлено» показывало
-- `theme.createdAt` — момент появления темы В СИСТЕМЕ, одинаковый для всех
-- магазинов (у всех, кто выбрал Bloom, стояло «15 апреля 2026»). Нужна дата
-- выбора конкретным магазином, а её нигде не хранили.
--
-- Задним числом восстановить нечего: событий смены темы мы не писали. Поэтому
-- у существующих магазинов колонка остаётся пустой, а интерфейс показывает
-- запасное — дату создания магазина. Новые выборы пишутся точно.
ALTER TABLE "site" ADD COLUMN IF NOT EXISTS "theme_applied_at" timestamp;
