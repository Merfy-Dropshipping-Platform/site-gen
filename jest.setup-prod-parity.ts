/**
 * Тесты = прод: на проде у всех сайтов включён режим «как у верстальщиков»
 * (PARITY_DESIGN='*'), поэтому и в jest он включён по умолчанию — иначе
 * page-blocks `designParityFlag` в спеках не ставит признак, и проверки
 * смотрят на ветку, которую покупатель не видит. Спеки, которые проверяют
 * сам выключатель, задают PARITY_DESIGN явно — это значение уважается.
 * Рендеры спек (дочерние процессы) делают то же в prod-design-parity.mjs.
 */
if (process.env.PARITY_DESIGN === undefined) {
  process.env.PARITY_DESIGN = "*";
}
