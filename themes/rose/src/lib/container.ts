/**
 * Канон горизонтальной сетки темы rose — ОДИН источник чисел.
 *
 * Вся витрина rose (секции главной, подвал, панель поиска) выровнена по двум
 * уровням:
 *   1. ВНЕШНИЙ рельс — лестница боковых отступов по брейкпоинтам
 *      (`ROSE_RAIL_PAD`). Именно она держит контент на отступе от края экрана.
 *   2. ВНУТРЕННИЙ рельс — предельная ширина контента 1320px, центрированная
 *      (`ROSE_RAIL_INNER`). Именно она задаёт вертикали, на которые встают
 *      заголовки секций, карточки и ссылки подвала.
 *
 * Пропуск второго уровня не виден, пока `ширина_экрана − 2 × отступ ≤ 1320`
 * (в том числе ровно на 1440), и вылезает на широких экранах: элемент без
 * рельса растягивается до самой лестницы отступов и оказывается ШИРЕ секций.
 * Так и было со шапкой (баг тестировщика 14.09): на 1512 она выходила за
 * секции на 16px с каждой стороны, на 1920 — на 20px.
 *
 * Числа не выдуманы: они сняты с секций главной
 * (`src/components/sections/*.astro`) и подвала (`src/components/Footer.astro`),
 * то есть с самой темы. Гард `src/themes/__tests__/rose-header-grid.spec.ts`
 * сверяет этот модуль с исходниками секций и роняет сборку при расхождении.
 *
 * ВАЖНО: строки классов — ЛИТЕРАЛЫ. Tailwind сканирует исходники статически;
 * склейка класса из переменных (`max-w-[${N}px]`) утилиту не породит.
 */

/** Лестница боковых отступов рельса (брейкпоинты Tailwind по умолчанию). */
export const ROSE_RAIL_PAD =
  "px-4 sm:px-5 md:px-10 lg:px-16 xl:px-20 2xl:px-[280px]";

/**
 * Лестница отступов для рядов, живущих только ниже `lg` (мобильная строка
 * шапки, мобильные шторки). Выше `lg` такие ряды скрыты, поэтому старшие
 * ступени им не нужны — подмножество `ROSE_RAIL_PAD`.
 */
export const ROSE_RAIL_PAD_MOBILE = "px-4 sm:px-5 md:px-10";

/** Предельная ширина внешнего блока — как у подвала темы. */
export const ROSE_RAIL_OUTER_MAX_WIDTH = 1920;

/** Предельная ширина контента — вертикали, по которым стоит вся витрина. */
export const ROSE_RAIL_MAX_WIDTH = 1320;

/** Внешний рельс: центрированный блок с лестницей отступов. */
export const ROSE_RAIL_OUTER =
  "mx-auto w-full max-w-[1920px] px-4 sm:px-5 md:px-10 lg:px-16 xl:px-20 2xl:px-[280px]";

/** Внутренний рельс: центрированный контент предельной ширины 1320px. */
export const ROSE_RAIL_INNER = "mx-auto w-full max-w-[1320px]";
