# AI instructions

Read and follow the package-specific rules in `CLAUDE.md` in this directory in
addition to the repository-level `AGENTS.md`. This adapter exists so Codex and
Claude Code use the same legacy package instructions while they are migrated.
