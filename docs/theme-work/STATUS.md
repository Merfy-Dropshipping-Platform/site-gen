# Статус тем Merfy

Канон для агентов. Обновлять после каждого законченного куска по теме.
Правило: `.claude/rules/record-theme-work.md`, `AGENTS.md`.

**Последнее обновление:** 2026-09-07 (контракт секций + автопроверка `scripts/section-contract-check.mjs`; bloom проходит, у остальных тем 24–30 нарушений)
**Контракт секций (обязателен для новых тем):** `docs/theme-work/SECTION-CONTRACT.md` — 12 правил + гейт `node scripts/section-contract-check.mjs <тема> [--runtime --site <id> --baseline rose --baseline-site <id>]`
**Эталон поведения (конструктор: сайдбар секций, настройки, cart/checkout/wishlist, динамические страницы):** `flux` — USER_CONFIRMED
**Визуал темы:** порт верстальщика `themes/<t>` + github `Merfy-Dropshipping-Platform/<t>-theme`
**Вне scope:** `luna`

| Тема | Статус | Эталон? | Следующий шаг |
| --- | --- | --- | --- |
| rose | исторический reference | визуал/ранние волны | не трогать без запроса |
| flux | USER_CONFIRMED готов по эталону поведения | **да, текущий** | не расширять скоуп без запроса |
| satin | главная в конструкторе = состав Satin-theme | нет, догоняет flux | cart, checkout, wishlist, динамические страницы, сайдбары как у flux |
| vanilla | не эта волна | нет | ждать явный запрос |
| bloom | WIP — аудит 10+ секций главной | нет | product + catalog после главной |

---

## flux

- **Статус:** USER_CONFIRMED 2026-08-30 — «сделал флукс по нашему эталону».
- **Сделано (по словам пользователя, независимой приёмки в этой сессии не было):** секции и их сайдбары, поведение секций и настроек, корзина, чекаут, избранное, динамические страницы.
- **История:** spec 109/111, layout-parity, constructor-live-markup, conformance. Детали в старых memory `project_flux_*`.
- **Не трогать:** чужие темы, shared `theme-base` без нужды.

## satin

- **Статус:** WIP. Конструктор `:3200`. Главная выровнена с репой верстальщика `Merfy-Dropshipping-Platform/Satin-theme` (`index.astro`). Сайдбары home+addable секций сверены с live Rose puck-config — MATCH. Header раскладка (logoPosition×4, menuType sidebar/dropdown/mega) = контракт rose-порта, визуал satin. Catalog card knobs ещё нет.
- **Конструктор (local):** `http://localhost:3200/?siteId=57ac2ede-853d-42f3-abd5-68d6bbbc0709`
- **Учётка:** `satin-e2e-test-local@example.com` (не логиниться в том же Chrome-профиле, что flux).
- **Site:** `57ac2ede-853d-42f3-abd5-68d6bbbc0709`, theme `satin`, status `draft` (был `frozen` — разморожен).
- **Состав главной = verstka:** Header → Hero → Collections → Popular → MultiRows → MainText → ImageWithText → Collections-2 → Popular-2 → MultiColumns/Features → Publications/Journal → FAQ → Footer.
- **Пруф:** CSS портов в превью (`dist/theme-css/satin.css`): Hero 680px, чёрный CTA, Kelly Slab, сердца 32px, фото коллекций/товаров. Локальный стек — worktree `sites/.worktrees/flux-constructor-live-markup`.
- **Скоуп (как у flux, ещё открыт):** добить поведение Header menuType и Catalog/Popular card knobs; корзина, чекаут, избранное, динамические страницы.
- **Гочи:** сайт был frozen + trial `past_due`; billing на :3112. Не логиниться в профиле flux. Правки sites в main checkout без рестарта worktree на превью не попадают.
- **Открыто:** cart/checkout/wishlist/динамика до flux; в pages нет wishlist.
- **Секция MultiRows (2026-08-31):** вёрстка CollectionRows лежит в настройках рядов (Женская/Мужская коллекция + CTA в каталог). Дальше IWT/Features/Journal/FAQ.
- **Схема и подписи (2026-08-31):** как rose/flux: scheme-1 и scheme-2 светлые, контент на `scheme-2`, чёрная палитра = scheme-4 (промо/копирайт). Инжект конструктора полный (`:root` + `.color-scheme-N`). Строки секций по-русски (Шапка/Подвал/Изображение).
- **2026-09-07 — контракт секций: satin `✓ соблюдён` (44 нарушения → 0, VERIFIED прогоном).** Прогон `scripts/section-contract-check.mjs satin --runtime --site 57ac2ede… --baseline rose --baseline-site e03dd420…`. Закрыто: §7 форматирование «Ж»/«К» — новый `themes/satin/src/lib/rich-text.ts` + `inlineFormat` в 28 текстовых полях 14 секций, в `global.css` добавлены `strong,b{700}` / `em,i{italic}`; §4 своя цветовая схема на корне — 12 секций (в т.ч. CollapsibleSection/ImageWithText/MultiColumns/MultiRows/Slideshow, которые чекер ПРОПУСКАЛ из-за наличия `containerColorScheme`), разбор схемы строкой И числом в Header/Footer/Slideshow; §5 тумблеры `agreement`/`hideTitle` в Newsletter; §11 «Положение видео» — видимое поле стало приоритетнее скрытого legacy `align` из blockDefaults; §6 «Размер заголовка/текста» рассылки в подвале — лестницы small(канон 14/12px)→medium→large, раньше размеры не читались вовсе; §9 карточка каталога — «Стиль кнопки», «Вид изображения», «Следующее фото при наведении» проведены во все три пути рендера (SSR-карточка, SSR-плейсхолдер, `renderCardHtml` + hover-swap), дефолт `Catalog.productCard.buttonStyle` переопределён на `primary` = канон satin (чёрная кнопка); §3 — `blockPlaceholders` для 8 блоков в `packages/theme-satin/theme.json`, заглушки портов CollapsibleSection/Newsletter/ContactForm выровнены с текстом панели («ВОПРОСЫ И ОТВЕТЫ» / «Подпишись на новости» / «Связаться с нами»).
- **Пруфы (POST `/preview/block`):** `<strong>` в заголовке Галереи; `color-scheme-4` на корне Популярного; Видео contained ≠ fullscreen (разные md5); подвал `text-xs` → `text-[16px]`; каталог `color-button-secondary-bg` и `aspect-square`. Пересобрано: `compile-theme-sections.mjs satin`, `compile-astro-blocks.mjs satin`, `npx nest build` + рестарт sites.
- **2026-09-07 (актуализация по лекалам bloom-сессии).** (1) Секция «Публикации» satin читала ТОЛЬКО `merfy.catalog.publications`, а публикации вынесены из `storefront-data` в отдельный `/api/sites/:id/publications` (`storefront-data.controller`) — поле приходило пустым, и секция НИКОГДА не показывала реальных записей. Добавлен SSR-запрос эндпоинта (порядок: legacy `posts[]` → резолв платформы → fetch → `publications.json`). Проверка тем: flux/vanilla/bloom запрос уже имели, **satin была единственной отстающей**; у rose своей секции в `themes/rose` нет. (2) Сайту засеяны 3 публикации в манере satin («Базовый гардероб на сезон», «Как ухаживать за трикотажем», «Новая коллекция: тихая роскошь», status `published`) — прямо в `publications`, `organization_id` = `site.tenant_id`, потому что создание через gateway требует сессии мерчанта. Пруфы: секция рендерит 3 карточки; «Дата и время» вкл → 3 даты, выкл → 0; «Карточки» 1 → 1, 3 → 3. (3) Охват прогона satin — **218 полей в 32 блоках из 34** (у bloom 219/32; без полей — «Итоги корзины» и «Кнопка оформления»); товаров с непустыми вариациями у satin 50 из 50, поля вариаций проверяются. (4) Перепрогон после платформенных правок соседней сессии (`resolve-props`: «не задано» = только пустое; фикс клика по ссылке в превью) — регрессий нет, `✓ satin: контракт секций соблюдён`.
- **Правило от пользователя (2026-09-07, из bloom-сессии): состав панелей и сайдбаров НЕ менять.** Если поле кажется недоступным — искать причину в выборе/подсветке, а не выносить поля в панель. Наши правки satin состав панелей не трогали (порты + `theme.json` + плейсхолдеры инпутов).
- **2026-09-07 — каталог и страница товара satin (лекало bloom-сессии).** Фильтры каталога живые: «Размер» (S/M/L/XL), «Цвет» (8 значений), цены 990–16489, 60 товаров; `Цвет=Розовый` → 11, `Цвет=Чёрный` → 22, `Размер=M` → 48, цена 2000–3000 → 4, поиск `q=носки` → 1, `limit=3` → 3 из 60, сортировка `price_asc/price_desc` монотонна. Блок «Каталог»: сетка, карточки, панель фильтров, сортировка, пагинация, кнопка в корзину, избранное; «Вид фильтра» top ≠ side. **Найдено и починено:** заголовок и подзаголовок каталога экранировались (`&lt;strong&gt;`) — добавлен `packages/theme-satin/blocks/Catalog/rich-text.ts` (block-папка автономна, sibling-копия как в bloom) и `set:html={inlineFormat(...)}` на `categoryTitle`/`categorySubtitle`. Страница товара (общий порт `theme-base/blocks/Product`): название, медиа, обе группы вариантов, опции, свотчи, цена 12990 + старая 15588, кнопка в корзину — всё на месте; «Вариации» circle → 8 `rounded-full`, square → 0; «Стиль» button ≠ dropdown. Контракт после правок: `✓ satin соблюдён`.
- **Тестовое состояние остатка (satin QA, 2026-09-07):** у товара «Худи «Лонг»» (`52dae7f4-ef3c-45b8-a66d-590157188b65`) комбинации `KNT-12-S-ROZ` (`d6a3376a-8fc4-4832-8022-4db2ec37bc9a`) обнулён остаток — **было `quantity = 9`**, стало `0`, чтобы в браузере проверялось «Нет в наличии». Вернуть: `UPDATE product_variant_combinations SET quantity = 9 WHERE id = 'd6a3376a-…'`. Фильтр `availability=sold_out` при этом остаётся пустым — он работает на уровне ТОВАРА, а товар доступен другими комбинациями (корректное поведение, не баг).
- **Наблюдение (не чинил, вне satin):** кнопки «Избранное» на странице товара нет НИ У ОДНОЙ темы — порт `Product` общий (`packages/theme-base/blocks/Product/Product.astro`, 0 вхождений `data-wishlist-toggle`). В карточке каталога избранное есть. Решение по общему порту — за пользователем.
- **2026-09-07 — догон bloom-сессии по satin (демо-товары, корзина, «Заказ оформлен»).** (1) Требование пользователя «никаких демо-товаров верстальщика, заглушки темы только когда товаров нет вообще»: в satin `Catalog.astro` моки уже удалила bloom-сессия (коммит `61d4f00c`), но **не собрала и не проверила** — пересобрал (`astro build` в `themes/satin` + `run-theme-build.ts satin`, по её же гоче: `compile-theme-sections` для не-Puck страниц мало) и проверил. (2) **Найдено и починено самим:** `themes/satin/src/pages/cart.astro` рисовал 4 товара верстальщика из `data/products` без гидрации + фиктивную `NtPagination` (`totalPages=3` от длины мок-массива). Теперь SSR-заглушки платформы (`/placeholders/sweater-*.svg`) + гидрация реальными товарами через `loadRealProducts`/`renderCardHtml`; пагинация убрана. Пруф по сборке: `themes/satin/dist/cart/index.html` — демо-ассетов 0, заглушек 12, маркер `data-nt="cart-recommended"` есть. (3) Добавлена страница «Спасибо за заказ»: `packages/theme-satin/pages/checkout-result.json` + запись `page-checkout-result` в `theme.json` (у satin её не было, как и у vanilla; у rose/bloom/flux есть). Блоки `CheckoutHeader`/`OrderConfirmation` у satin доступны; рендер проверен — приветствие, подтверждение, детали заказа, кнопка возврата. (4) Проверено и **правки не потребовалось**: адрес/контакты футера satin НЕ захардкожены (берутся из `socialColumn.contactFields`), фикс правовых ссылок `/legal/<slug>` в общем `src/utils/footer-data.ts` satin получил автоматически, секция «Список коллекций» satin уже на заглушках. (5) Демо-ассеты по всей сборке: satin — только иллюстрации страниц «О нас»/«Контакты» (`/images/4x/История_*`, `Контакты.*`), в каталоге **0**; для сравнения у bloom на момент проверки 5 страниц, включая `catalog/index.html` (15 вхождений) — её открытый хвост. Контракт после всего: `✓ satin соблюдён`.
- **Мои satin-правки вошли в общий коммит bloom-сессии `61d4f00c`** (`origin/flux-constructor-live-markup`, 156 файлов) — она пушила всю ветку по просьбе пользователя.
- **2026-09-07 — второй догон bloom-сессии по satin (auth, служебный slug, контентные страницы).** (1) **Мёртвые формы верстальщика:** `themes/satin/src/pages/auth/{sign-in,sign-up,forgot-password}.astro` были отдельными формами С ПАРОЛЕМ и заглушкой `alert("Демо-вход: авторизация ещё не подключена")` — вторая, нерабочая форма входа рядом с живой passwordless-парой `/login` + `/register`. Заменены редиректами (паритет bloom). Пруф по сборке: `url=/login`, `url=/register`, полей пароля — 0. (2) **Служебный slug коллекции:** satin отбрасывал только `_placeholder`, а `preview` (системный пресет страницы коллекции в конструкторе) уходил в API как настоящий `collection_id` и ронял запрос — страница коллекции показывала «Не удалось загрузить товары». Добавлен общий хелпер `isPlaceholderCollection` со списком `["preview","_placeholder"]` в обе ветки (`currentCollectionRef`, `isCollectionCatalogWithoutPick`) — паритет bloom. (3) **Контентные страницы:** «О нас», «Доставка», «Контакты» у сайта были ПУСТЫЕ; на «Контактах» стоял пустой блок «Страница», хотя тема satin (как rose/flux/bloom) закладывает `ContactForm`. Залит текст в манере satin (одежда) в «О нас» (238 симв.) и «Доставку» (317 симв.), блок «Контактов» заменён на `ContactForm`. Скрипт пропускает страницу, если у мерчанта уже есть свой текст; бэкап ревизии — `/tmp/satin-revision-backup.json` (ревизия `e97114ea-8ff6-4ce6-a5db-458ec83ab477`). (4) **Ленивый сид проверен:** после её платформенного фикса (`page-resolver-instance.ts` искал пакеты в несуществующем `dist/packages/theme-<id>`; `page-blocks.ts` отдавал весь файл вместо массива блоков) страница `page-checkout-result`, которой НЕТ в ревизии satin, открывается из пакета темы. Пруфы превью: about → «О нас», contacts → «Связаться с нами», delivery → «Доставка и оплата», checkout-result → «Спасибо за заказ» / «заказ подтверждён». (5) Не потребовалось: колонки СДЭК в `orders`/`order_items` она добавила в общую БД — satin получил автоматически. Контракт: `✓ satin соблюдён`.
- **Не коммичено** (по правилу — без явной просьбы). Правки: `themes/satin/src/**`, `packages/theme-satin/{theme.json,blocks/Catalog/*}`.

## rose

- Исторический эталон визуала и ранних волн конструктора.
- Не считать текущим эталоном поведения, пока пользователь не скажет иначе.

## vanilla / bloom

### bloom (2026-08-31)

- **Статус:** WIP. Аудит секций вне home seed (10 шт.) — runtime preview/block **10/10 OK**.
- **Site QA:** `10df1c3a-a1fc-45e0-957a-511c131b1eb8`, constructor `http://localhost:3200/?siteId=...&page=home`
- **Проверено (POST /preview/block):** Collections, ImageWithText, MultiRows, CollapsibleSection, Newsletter, Slideshow, Publications, Video, ContactForm, CartSection.
- **Фиксы wiring:** `page-blocks.ts` (IWT headingSize, Collections subtitle, MultiRows/Video normalize, Newsletter alignment→position); `preview.service.ts` adaptLegacyProps в hot-render; MultiRows `secondary`→white pill.
- **BY DESIGN:** Bloom Collections = сетка товаров (не плитки коллекций flux/rose); puck label «Список коллекций» — legacy имя.
- **2026-09-06 — размеры в Popular («Коллекция товаров») починены (VERIFIED в конструкторе):** ветка `small` перекрывала канон DS на 18px, а `small` = дефолт панели → любая правка секции делала заголовок меньше подзаголовка; ступени текста (19/23) были меньше дефолтных 24px. Приведено к `MANNER.md` §9: заголовок small=канон 20/24/32, medium 38, large 46; текст small=канон 14/18/24, medium 29, large 35. Аудит остальных 17 секций bloom — лестницы монотонны, Popular была единственной сломанной.
- **⚠️ Гоча контура:** без запущенного `user`-сервиса (:3111) `GET /api/sites/<id>` висит вечно → конструктор остаётся на **rose** puck-config (`SiteConstructor.tsx` `themeId ?? "rose"`) и показывает чужие поля/дефолты + рассинхрон панель↔превью. Проверять `./flux-baseline/local-stack.sh status` ПЕРВЫМ делом.
- **Пересборка после правки порта:** `node scripts/compile-theme-sections.mjs bloom` **и** `npx tsx theme-registry/_build-theme.ts bloom` — без второго новые arbitrary-классы Tailwind не получают CSS-правил в превью.
- **2026-09-06 — форматирование текста («Ж»/«К») починено во ВСЕХ секциях (VERIFIED):** кнопки конструктора оборачивают значение в `<strong>`/`<em>`, Astro экранировал → в превью были сырые теги. Подключён `inlineFormat` (rich-text.ts, XSS allow-list) во всех 14 секциях + Footer + Catalog; новый `ui/BloomSectionHeading.astro` (разметка DS 1:1 + set:html) вместо внешнего `NtSectionHeading` в Popular — приём rose. В `global.css` добавлены `strong,b{font-weight:700}` / `em,i{font-style:italic}` — без них `bolder` от лёгкой базы (300) давал 400 и жирный был неотличим. Заодно закрыт XSS в местах, где `set:html` шёл сырым.
- **2026-09-06 — Footer: поля рассылки ожили.** Заголовок/текст блока подписки были захардкожены → панель («Заголовок»/«Текст» = `heading.text`/`text.content`) не работала. Fallback на литералы верстальщика сохранён.
- **2026-09-06 — приоритет размера выправлен в 5 секциях** (CollapsibleSection, ContactForm, Gallery, MultiRows, Publications): панель пишет top-level `headingSize`, порты читали legacy `heading?.size` первым → при старой ревизии селект глушился. Hero/Newsletter/MainText оставлены nested-first (там панель реально objectField — сверено по `/api/themes/bloom/puck-config`).
- **2026-09-06 — «Вид изображения» был МЁРТВ в Popular и Collections, починен (VERIFIED):** перекрытие шло селектором `[&_li>article>a]`, а разметка — `li>article>div>a`; плюс `BloomProductCard` не принимал `aspectRatio`, а гидрация звала `renderCardHtml(p,"1/1")`. Теперь inline `aspect-ratio` во всех трёх путях (SSR-карточка, плейсхолдер, гидрация через `data-card-aspect`) — схема rose. Пруф: Портрет 244×284, Широкий 244×137, Квадрат 244×244.
- **⚠️ Гоча сборки:** правка ВЛОЖЕННОГО компонента (`products/BloomProductCard.astro`, `lib/*`) требует **рестарта sites** — dist пересобран, но модуль уже в кэше импортов Node. Секции `sections/*.astro` подхватываются без рестарта.
- **Паритет с rose (сверено программно):** 34 секции в обеих темах, ключи/подписи совпадают; поля, опции и дефолты `Collections`/`PopularProducts` идентичны. «Следующее фото при наведении» — механика 1:1 с rose. **«Быстрое добавление» доведено до rose 2026-09-06 (VERIFIED):** решение пользователя — вёрстка верстальщика это лишь одно из состояний, настройка обязана работать одинаково во всех темах. Теперь `none` — CTA карточки нет, `standard` — кнопка (`data-quantity="1"`), `cart` — степпер «− N +» перед кнопкой (механика rose 1:1: делегат на гриде, `data-qa-step-bound`, правит `data-quantity`). Отсутствие пропа = `standard`, поэтому дефолтный вид темы не поехал. Пруф в конструкторе: Нет → CTA 0; Стандарт → CTA 6, степперов 0; Количество → CTA 6, степперов 6; клик по степперу 1→2→3→2 с синхронным `data-quantity`. Побочно: «Стиль кнопки» применялся только к `[data-add-to-cart]`, а у товара с вариантами CTA — ссылка на PDP; обе формы помечены `data-card-cta`.
- **2026-09-06 — рассинхрон «в превью заголовок есть, в поле пусто» закрыт (9 → 0, VERIFIED):** платформа считает плейсхолдерные строки незаполненным полем (`render/empty-state.ts` + `resolve-props.ts` вычищают их), а порт рисует заглушку → панель пустая. Записать заглушку в данные НЕЛЬЗЯ (вычистится). Введена возможность темы **`blockPlaceholders`** в `theme.json` (`{"Gallery":{"heading":"Галерея"},"Hero":{"heading.text":"Изображение"}}`, путь `a.b` для objectFields): `theme-puck-config.controller` кладёт текст в `placeholder` поля и очищает конфликтующий universal-дефолт. Проброс сделан всем 5 темам, заполнен пока только bloom. Плюс 7 портов bloom переведены на `(...)?.trim() || "Заглушка"` — пустая строка теперь тоже «не задано». Пруф: сверка панель↔превью 13/13 OK; заданный текст не подменяется; главная рендерится прежними заголовками.
- **Аудит настроек (POST /preview/block):** «Размер заголовка»/«Размер текста» живые во всех секциях, где поля есть; у MultiColumns/MultiRows они на элементах (`columns[]`/`rows[]`), у Slideshow — на `slides[].heading.size`, у Video поле зовётся `size`. Карточка Popular: buttonStyle / imageView / nextPhotoOnHover / quickAddMode / columns / padding — все живые.
- **2026-09-07 — сплошной аудит настроек: 141 поле × 17 секций, отставаний от rose 0 (было 9).** Метод: `.tmp-bloom/audit-fields.py` — каждое видимое поле рендерится с двумя значениями через `POST /preview/block` на bloom И на rose (`e03dd420-…`), результаты сравниваются. Починено: Footer (colorScheme, contentAlign, heading.size, text.size), Video (position приоритетнее legacy align + схема), Publications (схема), ImageWithText (alignment), PromoBanner (вариант «Тонкий» отсутствовал → падал в «Большой»), MainText (text.size приоритетнее скрытого top-level), Newsletter (agreement принимает boolean).
- **2026-09-07 — «поменял одну секцию, а соседние перекрасились» — корень найден и закрыт:** `colorScheme` ставила ТОЛЬКО обёртка композитора, при hot-render одиночного блока её нет → блок брал `:root`. Теперь каждая секция bloom (14 файлов) ставит свою схему сама. Гоча: у Slideshow платформа приводит схему к ЧИСЛУ — строковая проверка молча не срабатывала.
- **2026-09-07 — платформенный фикс (закрыто):** «поле не заполнено» больше не определяется словарём служебных слов — теперь это «пусто ИЛИ ровно дефолт блока» (`src/render/block-defaults.ts` + `isBlankContent`). Мерчант может назвать секцию «Видео» или «Коллекция», и текст не исчезнет; незаполненное поле по-прежнему показывает заглушку темы. Было (не чинить повторно): `render/empty-state.ts EMPTY_STATE_COPY` считает незаполненным полем обычные слова («коллекция», «кнопка», «заголовок», «видео», «публикации», «раздел»). Мерчант вводит «Видео» в заголовок → платформа вычищает, показывается заглушка. Вариант фикса: сузить `isUnsetContent` до пустой строки, а «равно дефолту» определять через уже поддержанный, но не передаваемый параметр `defaults`.
- **2026-09-07 — демо-товары верстальщика убраны из витрины bloom (VERIFIED).** Правило пользователя: демо-товаров верстальщика не должно быть нигде; заглушки темы показываются, ТОЛЬКО когда товаров в магазине нет вообще (а для каталога коллекции — пока коллекция не выбрана); пустая выдача фильтра — это «ничего не найдено», а не заглушки. Починены три места: (1) `packages/theme-bloom/blocks/Catalog/Catalog.astro` — 8 косметических моков (`/images/trend-*.webp`) заменены платформенными заглушками (свитеры + «Товар» + «2 500 ₽»), `catalog-demo` → `catalog-placeholder`; ветвление разведено на четыре случая (нет магазина → заглушки; API не ответил → «Не удалось загрузить товары»; `total=0` при непустом магазине → «Ничего не найдено»; магазин реально пуст → заглушки), «пуст ли магазин» определяется отдельным запросом без фильтров с кэшем; добавлена ветка «каталог коллекции без выбранной коллекции → заглушки, запрос не шлём». (2) `themes/bloom/src/components/sections/Collections.astro` — SSG-дефолт был `catalogProducts.slice(0,6)` (косметика), стал плейсхолдер платформы по лекалу `Popular.astro`. (3) `themes/bloom/src/pages/cart.astro` — блок «Возможно вам понравится» жёстко рисовал 4 товара верстальщика; теперь SSR-заглушки + гидрация реальными товарами магазина. Пруфы: сортировка «По новизне» + цвет «Белый» в коллекции «Макияж» → «Ничего не найдено. Попробуйте изменить фильтры.», 0 товаров (раньше всплывала косметика); превью с подменённым ответом API на пустой → 4 свитера-заглушки, счётчик 0; рекомендации корзины после гидрации → 4 реальных товара; `grep` косметики на превью home / page-catalog / page-collection / page-cart / product / checkout — 0. Пересборка: `compile-astro-blocks.mjs`, `compile-theme-sections.mjs bloom`, `npx astro build` в `themes/bloom` + `npx tsx scripts/run-theme-build.ts bloom` (без последнего в превью остаётся старая копия страниц темы в `dist/theme-preview/bloom`).
- **2026-09-07 — блок A чек-листа (динамические страницы + футер) закрыт, 4 бага починены.** (A1) Механика: 4 типа политик в `site_policy` + произвольные поля «Информации о компании» в `site_contacts`; у bloom и rose данных не было — bloom засеян двумя политиками (две оставлены пустыми) и контактами (телефон/почта/адрес/ИНН). (A2) Страницы политик у мигрированных тем генерирует `composeLegalPagesIntoDist` в маршрут `/legal/<slug>`. (A3) **Баг платформы, починен:** `src/utils/footer-data.ts` ставил ссылку `/<slug>`, а страница лежит в `/legal/<slug>` → ВСЕ правовые ссылки футера у всех пяти мигрированных тем вели на nginx-фолбэк. Путь теперь зависит от темы (мигрированная → `/legal/<slug>`, legacy-скаффолд → как было); тема прокинута в оба вызова `applyFooterData` (build + превью, включая hot-render футера — вызов перенесён после загрузки ревизии). (A4) **Баг bloom, починен:** адрес в футере был литералом «г. Москва, ул. Пушкина, д. 0» и висел всегда; теперь блок берёт поле «Адрес» из «Информации о компании» (разбивая по запятым), без поля не рендерится, остальные поля не дублируются. (A5) Телефон/почта/ИНН подтягиваются. (A6) Inline-редактирование блока «Страница» работает: заголовок и текст сохранились в ревизию (пруф — правка через focusout-агент, затем возврат страницы в исходное состояние). ⚠️ У сайта bloom сид контентных страниц не применён: `page-about`/`page-contacts`/`page-delivery` пустые (у flux — «О нас» + 180 симв). (A7) У bloom не было страницы «Спасибо за заказ»: добавлены `packages/theme-bloom/pages/checkout-result.json` и запись в `theme.json` (валидатор сидов: bloom 10 страниц, как rose/flux). **Попутно найдены и починены два платформенных бага ленивого сида страниц:** `src/themes/page-resolver-instance.ts` искал пакеты тем в `dist/packages` (такого пути нет ни локально, ни в образе — остальные модули используют `process.cwd()`), а `src/themes/page-blocks.ts` передавал дальше весь файл `{content, root, zones}` вместо массива блоков. Из-за этой пары ЛЮБАЯ страница, которой нет в ревизии сайта, не открывалась вовсе — теперь берётся из темы. Пруф: `page-checkout-result` у bloom отдаёт «Ваш заказ подтверждён»/«Детали заказа», поведение совпадает с rose; регрессий на home/catalog/cart/about/product/collection и на сайте rose нет.
- **2026-09-07 — личный кабинет и вход bloom проверены вживую, дубли-заглушки убраны.** ЛК у bloom уже был реализован (общая платформенная реализация, passwordless magic-link): `/login`, `/register`, `/reset-password`, `/account`, `/account/orders`, `/account/order`, `/account/profile` — файлы совпадают с rose построчно. Проверено НЕ по коду: создан тестовый покупатель `qa-bloom-buyer@example.com` (`store_customer` + `store_customer_session` с известным Bearer-токеном) и два заказа (`QA-1001` delivered 5490 ₽, `QA-1002` processing 2890 ₽). Пруфы: `/store/auth/me` отдаёт покупателя; `/store/orders` — оба заказа; в браузере «Мои заказы» рисует обе строки с почтой, «Основные данные» подставляет e-mail и телефон, «Заказ» показывает статус «Доставлен», номер, оплату и сумму 5 490 ₽; `/account/orders` без токена уводит на `/login?redirect=…`. **Убраны демо-заглушки верстальщика:** `auth/sign-in|sign-up|forgot-password` были отдельными формами с паролем и `alert("Демо-вход: авторизация ещё не подключена")` — заменены редиректами на живые `/login` и `/register` (две разные формы входа путали покупателя, а нерабочая — тем более). ⚠️ Страница «Спасибо за заказ» была критична: `CheckoutForm.successRedirectUrl = "/checkout-result"`, а у bloom этой страницы не существовало — покупатель после оформления попадал бы в никуда. **Локальный контур:** БД orders отставала от схемы кода — не хватало 6 колонок СДЭК в `orders` и 4 колонок габаритов в `order_items`, из-за чего падал ЛЮБОЙ select по заказам (детали заказа в ЛК и cron pending-orders). Колонки добавлены (`ADD COLUMN IF NOT EXISTS`, данные не тронуты), orders пересобран.
- **2026-09-07 — блок B чек-листа (динамические коллекции), 1 баг починен.** (B1) Страницы коллекций генерируются в v2-сборке: `collections/<slug>/index.html` + `collections/<id>/index.html` как копии шелла `collections/preview` с per-collection SEO (canonical/title/og). Пруф на собранном сайте flux: `/collections/aksessuary` → 200 «Аксессуары», несуществующая `/collections/novinki` → 404, есть `sitemap-collections.xml` с 6 коллекциями. (B2) «Смотреть все» в секции ведёт на `/catalog?collection=<ref>` — формирование ссылки в порте bloom построчно совпадает с rose. (B4) Пикер коллекции показывает 5 реальных коллекций магазина; после выбора «Уход за лицом» в секцию подставился её id, каталог показал 12 товаров коллекции, счётчик «12 товаров» (правка настройки после проверки снята из ревизии). (B5) Страница коллекции без выбранной коллекции показывает заглушки темы и «0 товаров». **Баг, починен:** служебный slug пресета (`data-collection-slug="preview"`, а также `_placeholder`) уходил в API как настоящий `collection_id` → запрос падал, и страница коллекции в конструкторе показывала «Не удалось загрузить товары» (до правок пустого состояния этот баг был замаскирован демо-товарами верстальщика — вместо ошибки показывалась косметика). Теперь `currentCollectionRef()` не отдаёт служебные значения, а `isCollectionCatalogWithoutPick()` считает их «коллекция не выбрана» → заглушки темы. (B3) **Открыто, нужно решение:** в пикере коллекций нет варианта «Все»/сброс — выбранную коллекцию нельзя снять из панели (снимал правкой ревизии).
- **2026-09-07 — хвосты блоков A/B закрыты.** (B3) Пикер коллекций сверен с rose ВЖИВУЮ (конструктор `e03dd420-…`, страница коллекции): у rose в списке тоже только реальные коллекции, варианта «Все»/сброс нет. Пикер — общий компонент конструктора `src/components/fields/CollectionPicker.tsx` (`onChange(collection.id)`, без пустого значения), поле `collectionPicker` объявлено одинаково во всех пяти темах → это поведение платформы, а НЕ отставание bloom. Решение о добавлении «Все» за пользователем. (A6) Контентные страницы сайта bloom наполнены сидом темы ТОЧЕЧНО (главную и остальные страницы не трогали, полный `admin-reset-content-pages` для этого не годится — он перезаписывает всё): «О нас» → заголовок + 116 симв, «Доставка» → заголовок + 214 симв. У «Контактов» на сайте стоял ПУСТОЙ блок «Страница», хотя по сиду темы (и у rose, и у flux) там форма обратной связи — блок заменён на `ContactForm`. Пруф: превью трёх страниц отдаёт «О нас», «Связаться с нами», «Доставка». Скрипт пропускает страницу, если у мерчанта уже есть свой текст.
- **2026-09-07 — блок C чек-листа (общие настройки темы) проверен кликами, 1 баг починен.** Панель «Настройки темы» bloom содержит 19 настроек (логотип, цвета, типографика, страница, кнопки, поля ввода, карточки товара, медиа, соцсети, корзина); набор `themeSettings` совпадает с rose поле-в-поле (36 ключей). Механическая сверка через `POST /preview/tokens-css` (каждая настройка с двумя значениями): у bloom и rose ОДИНАКОВО — 18 настроек меняют tokens.css, 7 нет. Из этих семи `containerMaxWidth`, `gridRowGap`, `gridColumnGap`, `heroButtonHeight`, `newsletterFormWidth`, `buttonStyle` — легаси-ключи БЕЗ контролов в панели (мерчант их не видит; токены читают только дефолт темы), `cardBorder` пишется лишь при стиле карточки «card». Проверено кликами в конструкторе с замером computed-стилей: **насыщенность заголовка** 300→700 (h2 фактически 700), **скругление кнопок** 100→8 (кнопка «Смотреть каталог» 8px), **скругление медиа** 12→0, **отступ между секциями** 0→120 (прямые дети `<main>` получают margin-top — правило `main > * + *` из tokens-css), **цвет схемы 2** #e38e9f→#00AA00 (секция перекрасилась), **соцсети** (Telegram → ссылка в футере), **размер логотипа** 12→40 (высота логотипа следует токену). **Баг, починен:** карточки товара, отрисованные КЛИЕНТСКОЙ гидрацией, игнорировали настройку «Медиа → Скругление» — `rounded-[12px] bg-[#F5F5F5]` хардкодом вместо токенов. Заменено на `rounded-[var(--radius-media,12px)] bg-[rgb(var(--color-surface,…))]` в 6 местах: `lib/storefront-hydrate.ts` (renderCardHtml), `packages/theme-bloom/blocks/Catalog/Catalog.astro`, `Popular.astro`, `Collections.astro`, `BloomProductCard.astro`, `lib/cart.ts`, `pages/cart.astro`, `pages/wishlist.astro`. Пруф: при mediaRadius=0 карточки стали 0px (раньше оставались 12px). ⚠️ У rose в `storefront-hydrate` тот же хардкод (`rounded-[8px] bg-white`) — тот же гэп, не чинили (вне темы работы). ⚠️ Ползунок «Кнопки → Скругление» имеет max 48, а дефолт темы bloom = 100 (pill): сдвинув ползунок, вернуть исходный вид из панели уже нельзя. Все тронутые настройки возвращены к исходным значениям (сверено по ревизии).
- **2026-09-07 — паритет bloom↔rose добит, открытые пункты чек-листа закрыты.** Сверка puck-config: **34 блока в обеих темах, наборы совпадают, блоков с расхождением полей — 0**; `section-contract-check.mjs bloom` → контракт соблюдён. (1) **Ползунок «Кнопки → Скругление» расширен 48 → 100** (`ThemeSettingsPanel.tsx`): дефолт bloom `--radius-button: 100px` (pill) не влезал в прежний потолок, и мерчант, тронув ползунок, не мог вернуть родной вид темы из панели — приходилось править ревизию. У остальных тем дефолты 0–6px (rose 6, flux 4, satin 0, vanilla 0), расширение их не задевает. Пруф: ползунок 0–100, значение 100, токен `--radius-button: 100px`. (2) **Вариант «Все» в пикере коллекций НЕ добавляем** — в rose его нет (проверено вживую), это поведение платформы, а не отставание bloom. Два места, где bloom теперь ЛУЧШЕ rose и откатывать не стали: медиа-радиус в клиентских карточках (у rose остался хардкод `rounded-[8px] bg-white`) и страницы `/auth/*` (у rose — мёртвые формы с `alert("Демо-вход…")`, у bloom редиректы на живые `/login`/`/register`).
- **Открыто:** product page, catalog, sidebar controls click-audit, 402 upload paywall. Остаточные поля (не баги bloom, паритет с rose): MultiColumns.imageAspectRatio (нужна картинка), MultiRows.size (перекрывается размером ряда), Publications.showDateTime (нужны публикации), PromoBanner.padding (Figma убрала — скрыть поле), Footer.socialColumn.title (нет в вёрстке). Расхождение Benefits: реестр рендерит простую `packages/theme-bloom/blocks/Benefits`, а богатый порт `themes/bloom/.../Benefits.astro` не используется.

### vanilla

- Не эта волна. Ждать явный запрос.
