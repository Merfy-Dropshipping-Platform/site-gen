# Локальная витрина :8099.
# Полный конфиг: flux-baseline/nginx-local-live.conf
# (прокси /api → gateway:3110, __MERFY_API_BASE__=location.origin).
# НЕ копировать в прод-шаблон nginx-minio-proxy.
#
#   docker run -d --name merfy-local-live --network merfy-network -p 8099:80 \
#     -v "$PWD/flux-baseline/nginx-local-live.conf:/etc/nginx/conf.d/default.conf:ro" \
#     nginx:alpine
# Пруф: node theme-registry/catalog-live-api-proof.mjs
