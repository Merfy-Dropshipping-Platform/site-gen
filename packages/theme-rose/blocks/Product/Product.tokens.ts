// Rose Product — re-export базового CSS-var whitelist (анатомия блока). Родной
// рендер rose использует те же --color-*/--radius-* токены (hot-swap схем).
export { ProductTokens } from "../../../theme-base/blocks/Product/Product.tokens";
